/** Includes the main class that coordinates simulation.
 * The "Controller" class implements the network controller that encodes the distributed algorithm.
 * The "Agent" class that implement that agent that runs in network nodes. */
package controller;